'use strict'

const arithmetic  = require('../lib/arithmetic.js').arithmetic;

describe('add function', () => {
    test('it should return null if one of the nums are NaN', () => {
        let num1 = 9;
        let num2 = 'ajsdfj';
        let expected = null
    });
    test('it should return the sum of the numbers', () => {
      let num1 = 9;
      let num2 = 10;
      let expected = true;
    });
  });
